version: "2"

run:
  timeout: 5m
  tests: false

linters:
  default: none
  enable:
    - govet

formatters:
  enable:
    - gofmt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

output:
  formats:
    text:
      colors: true
